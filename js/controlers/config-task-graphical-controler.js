simsoApp.controller("GraphicalTaskControler",
["$scope", "confService", "$timeout", function($scope, confService, $timeout)
{
	// period, deadline, wcet
	// multiple tasks
	// size : all sim duration
	// gray non-period parts
	// drag drop center : activation date
	// drag and drop right : wcet
	// slide gray bar : period
	
	// Canvas : draw graduations and gray part
	// div : task
	// div : vertical bar
}]);